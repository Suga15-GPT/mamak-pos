# Codebase map

**Read this instead of exploring.** Every prompt in `docs/prompts/` names the files
it needs; this file tells you what is in them and roughly where. Keep it accurate —
every phase updates the rows it touches.

## Current state (after phase 07 — offline tolerance)

| File | Lines | Contains |
|---|---|---|
| `src/server.js` | 49 | Express app wiring only: json/static middleware, mounts the six route routers (now including `stream.js`), the `/t/:token` customer-page route, `/`, `/api/health`, and `boot()` (seed, session-cleanup interval, `app.listen`) |
| `src/lib/events.js` | 36 | **New in phase 06.** Tiny in-process event hub (one `EventEmitter`, no Redis/Postgres `LISTEN`): `publish(type, payload)` increments a monotonic `seq`, pushes onto a 200-entry ring buffer, and fans out to subscribers; `subscribe(fn)` returns an unsubscribe function; `recent(sinceSeq)` replays ring-buffered events after `sinceSeq` (falsy `sinceSeq` — a fresh connect, not a reconnect — replays nothing). Event payloads carry ids only (`order_id`, `table_id`), never the full order |
| `src/routes/stream.js` | 52 | **New in phase 06.** `GET /api/stream` (SSE), roles admin/staff/kitchen. Since `EventSource` cannot set an `Authorization` header, auth here accepts the bearer token via `?token=` as well as the header (a small local session lookup, mirroring `requireRole` rather than widening it). Replays `recent()` on connect using `Last-Event-ID` or `?since=`; heartbeats `: ping\n\n` every 25s; unsubscribes on `req.on('close')` |
| `migrations/006_idempotency.sql` | 6 | **New in phase 07.** Adds nullable `idempotency_key TEXT` to `orders` and `order_items`, plus a partial unique index on each (`WHERE idempotency_key IS NOT NULL`) so a client-generated key can never collide with another order/line but rows without one are unconstrained |
| `src/db.js` | 44 | Migration runner. Exports `pool`, `query`, `migrate()`. `migrate()` applies `migrations/*.sql` in filename order inside a transaction per file, tracked in `schema_migrations`; re-running applies nothing |
| `src/lib/errors.js` | 18 | `AppError(message, status)`, `awaitH` (wraps an authenticated route handler), `publicH` (same, but hides internal error text on public routes). **(Phase 05)** `awaitH` was silently swallowing every thrown error's `.status` and always responding 500 — fixed to respect `.status` (mirroring `publicH`'s existing correct behavior); discovered because it directly blocked this phase's own required "card overpay → 400" test on a route this phase owns (`POST /api/orders/:id/pay`), not a general cleanup |
| `src/lib/auth.js` | 43 | `hashPin`/`verifyPin` (scrypt), `requireRole(...roles)` — a plain Express middleware factory (`router.get(path, requireRole('admin'), awaitH(...))`) that looks up the bearer session and checks `SESSION_TTL`, `rateLimit(key,max,windowMs)` — an in-memory sliding-window limiter |
| `src/lib/money.js` | 26 | `cents2rm`, `rm2cents`, `roundCashCents`, plus (phase 02) `roundHalfUp` (0.5 away from zero), `lineTotal`, `computeBill({lines,taxRateBp,svcRateBp,discountCents,method}) -> {subtotal_cents, service_charge_cents, tax_cents, discount_cents, rounding_cents, total_cents}` (subtotal → svc → tax-on-subtotal+svc → discount → cash-only 5-sen rounding), `formatRM`. Pure, no DB — fully unit-testable |
| `src/routes/auth.js` | 28 | `POST /api/login` (rate-limited), `POST /api/logout` |
| `src/routes/public.js` | 60 | `GET /api/menu`, `GET /api/t/:token`, `POST /api/public/orders` (rate-limited QR ordering) — (phase 03) returns `201` on success and catches the same `one_open_order_per_table` `23505` race as `routes/orders.js`, returning `409 {error, order_id}` (a QR order does not yet write an `audit_log` row — only staff-created orders do). (Phase 04) `GET /api/menu`'s items query uses `services/orders.js`'s `ORDERABLE_SQL` instead of a bare `available` filter, and each item gets a `modifier_group_ids` array (from `item_modifier_groups`); groups now carry `min_select`/`max_select` |
| `src/routes/orders.js` | 303 | `GET /api/orders[?mode=recent]`, `GET /api/tables` (staff/kitchen, names only), `POST /api/orders`, `POST /api/orders/:id/items`, `POST /api/orders/:id/items/:lineId/void`, `PATCH /api/orders/:id` (status, `TRANSITIONS`/`BACKWARD` maps live here), `POST /api/orders/:id/pay` — (phase 02) recomputes the bill server-side from `order_items`/`order_item_mods` via `computeBill`, reads current `tax_rate_bp`/`svc_rate_bp` from `settings`, writes all eight money columns + snapshots the rates onto the order. (Phase 03) every mutating route writes attribution (`opened_by`/`added_by`/`paid_by`/`closed_by` = `req.user.id`) and one `audit_log` row via `writeAudit`; `POST /api/orders` returns `201` and catches the `one_open_order_per_table` unique-violation (pg code `23505`) to return `409 {error, order_id}` instead of 500; the void route sets `voided_at`/`voided_by`/`void_reason` (never deletes) — `staff` only while the order is still `sent`, `admin` any time before it's `paid`/`cancelled`; `TRANSITIONS` gained backward moves (`preparing→sent`, `ready→preparing`, `served→ready`) blocked for the `kitchen` role via the `BACKWARD` set. (Phase 05) `POST /api/orders`, `.../items` (also now 409s if the order already has a payment recorded, and threads an optional per-line `seat`) and the void route all call `services/billing.js`'s `recomputeOrderBill` after mutating, so `subtotal_cents`/`tax_cents`/`total_cents`/etc. are live even on open orders, not just at payment; `GET /api/orders` enriches each order with `payments` (from `listPayments`) and `amount_due` (from `amountDue`). `POST /api/orders/:id/pay` is rewritten onto `billing.addPayment` — body `{method, amount?, tendered?}`, `amount` (RM) defaults to the full remaining balance so the old "click a method to pay in full" flow is unchanged; response gained `change`/`remaining`/`settled` alongside the existing `bill: {...}` breakdown (kept for phase 02 compatibility, now reflecting the order's *current* stored columns post-payment, cash-rounding included on the settling leg). New: `GET /api/orders/:id/split?ways=N\|by=seat` (preview only, via `billing.splitEvenly`/`splitBySeat`), `POST /api/discounts/authorize` (`{name,pin}` → a short-lived one-use in-memory token authorizing one admin-approved discount for a `staff` caller — same in-memory-Map pattern as `rateLimit` in `lib/auth.js`), `POST /api/orders/:id/discounts` (`{kind,value,reason,authorize_token?}`, admin-only unless a valid token is supplied). **(Phase 05b)** the void route now previews the bill excluding the line being voided (via `billing.previewBillExcludingLine`) and guards against it dropping the total below what's already paid (`billing.guardAgainstShortfall`, `409`), then settles the order if the new total lands exactly on the paid amount (`billing.settleIfMatchesPaid`); new `DELETE /api/orders/:id/discounts/:discountId` (admin-only, `409` if the order already has a payment recorded) reverses a discount via `billing.removeDiscount`; `GET /api/orders` also enriches each order with `discounts` (from `listDiscounts`) so the payment modal can show each applied discount with its reason. **(Phase 06)** `POST /api/orders`, `.../items` (append), `PATCH /api/orders/:id` (status, including cancel), the void route, and `POST /api/orders/:id/pay` each call `lib/events.js`'s `publish()` once their writes have committed — `order.created`/`order.updated`/`order.updated`/`order.voided`/`order.paid` respectively (payload is ids only: `order_id`, `table_id`). **(Phase 07)** `POST /api/orders` and `POST /api/orders/:id/items` accept an `Idempotency-Key` header — a duplicate key returns the original result with `200` instead of erroring or creating a second row/lines; checked up front for a sequential retry, and by catching the `uniq_orders_idem`/`uniq_order_items_idem` unique-index violation (same pattern as `one_open_order_per_table` below it) for a concurrent retry. The append route derives one sub-key per line (`` `${key}:${index}` ``) since the unique index is one key per `order_items` row; the insert is one transaction, so a partial batch never persists |
| `src/routes/admin.js` | 185 | All `/api/admin/*`: menu CRUD, categories, modifier options, tables + QR PNG, users, (phase 03) `GET /api/admin/audit?limit=&entity_id=` — paginated `audit_log` joined to `users` for display name. (Phase 04) `GET /api/admin/menu` also returns `item_modifier_groups`; `PATCH /api/admin/items/:id` accepts `sort` and `sold_out_today` (sets/clears `sold_out_until` via a KL-timezone SQL literal, no `available` change — "sold out indefinitely" is still the existing `available` flag); new `PATCH /api/admin/categories/:id` (`name`/`sort`), `POST /api/admin/modifier_groups` (defaults `min_select`/`max_select` by `mode`, mirroring the migration's own backfill), `PATCH /api/admin/modifier_groups/:id` (`name`/`min_select`/`max_select`), `POST /api/admin/item_modifier_groups` (attach, `ON CONFLICT DO NOTHING`) and `DELETE /api/admin/item_modifier_groups/:itemId/:groupId` (detach) |
| `src/routes/reports.js` | 57 | `GET /api/summary` (dashboard SQL, `KL` timezone constant lives here), `GET|PATCH /api/settings` — (phase 02) now serves/accepts `{tax_rate_bp, svc_rate_bp}` (admin-only PATCH, validated 0-10000); the old fake `sst_on` boolean is retired (never read or written by any route) |
| `src/services/orders.js` | 139 | `buildOrderItems` (validates + prices a cart against the DB), `insertOrder`, `ordersWithItems` (joins orders+items+mods; `total` is still the live recomputed cart total for open orders) — shared by `routes/public.js` and `routes/orders.js`. (Phase 02) also returns the snapshotted bill breakdown (`subtotal`, `service_charge`, `tax`, `discount`, `rounding`, `grand_total`, `tax_rate_bp`, `svc_rate_bp`) — now populated as soon as `services/billing.js`'s `recomputeOrderBill` first runs on the order (phase 05), not only once paid. (Phase 03) `insertOrder(tableId, parsed, note, source, userId)` stamps `opened_by`/`added_by`; `ordersWithItems`'s live total excludes voided lines and each returned item now carries `id` (the `order_items` row id — needed to target a void) plus `voided`/`void_reason`; new `writeAudit(client, {userId, action, entityType, entityId, detail})` inserts one `audit_log` row (plain-object `detail` is serialised to `jsonb` by `pg` automatically). (Phase 04) exports `ORDERABLE_SQL` (a bare boolean SQL expression: `available AND` not sold out today in KL time — alias it in a SELECT list, or use bare in a WHERE); `buildOrderItems` now also loads each referenced item's attached `item_modifier_groups` and, per line, rejects any selected option whose group isn't attached to that item, and rejects any attached group's selection count outside `[min_select, max_select]` (message names the group, e.g. `"Kuah: choose exactly 1"`). **(Phase 05)** `buildOrderItems`'s parsed lines and `insertOrder` both carry an optional per-line `seat`; `ordersWithItems`'s returned items include `seat`. **(Phase 07)** `insertOrder` takes an optional trailing `idemKey`, written to `orders.idempotency_key` |
| `src/services/billing.js` | 283 | **New in phase 05.** `recomputeOrderBill(orderId)` rebuilds subtotal/service_charge/tax/discount/total from `order_items` + `discounts` using the *current* live settings rates and writes them onto the order — called after every mutation (create/append/void/discount) so open orders carry a live, accurate running total; never bakes in cash rounding (`rounding_cents` stays 0 here — see `addPayment`). `amountDue(orderId)` = `total_cents − Σ payments.amount_cents`. `hasPayments`/`listPayments`. `addPayment(orderId, {method, amountCents, tenderedCents, userId})` records one payment leg — `amountCents` defaults to the full remaining balance; non-cash over-amount is `400`; cash may request more than due (tendered can exceed the amount, yielding change), and if this leg settles the order the 5-sen cash-rounding adjustment (deliberately kept out of `recomputeOrderBill`) is folded into *this* leg and the order's stored `total_cents`/`rounding_cents` — settling also stamps the legacy `pay_method`/`pay_total_cents` columns (still summed by `routes/reports.js`'s dashboard) with whichever leg closed the order, since a split payment has no single true method. `addDiscount(orderId, {kind, value, reason, userId})` — `percent` (basis points) and `amount` (cents) resolve against the *undiscounted* subtotal per docs/REBUILD-PLAN.md §3 (tax stays computed on subtotal+service_charge, discount only reduces gross); `comp` zeroes the gross. `splitEvenly(totalCents, ways)` — floor + distribute the remainder sen-by-sen across the first shares, so `Σ shares === totalCents` always. `splitBySeat(orderId)` groups un-voided lines by `order_items.seat` (unassigned lines bucket under `'unassigned'`). **(Phase 05b)** `computeLiveBill(orderId, {excludeItemId, extraDiscountCents})` factors out `recomputeOrderBill`'s math into a preview that doesn't write — `previewBillExcludingLine` (void) and `addDiscount` (discount) both use it to see what the total *would* become before committing; `paidCentsFor(orderId)` sums `payments`; `guardAgainstShortfall(actionLabel, newTotalCents, paidCents)` throws `409` (with the exact "would leave RM X already paid against a RM Y bill — refund the payment first" message) if a void or discount would drop the total below what's already paid — a partially-paid order's status stays `sent`, so the paid-order status check never caught this; `settleIfMatchesPaid(orderId, totalCents, paidCents, userId, trigger)` closes the order (`paid`, `paid_at`, `closed_by`) and writes one `order.settle` audit row when a void or discount lands the total exactly on the paid amount (subsumes the old "comp zeroes the balance" close, which is just the paid-amount-is-0 case of the same rule). `listDiscounts`/`removeDiscount(orderId, discountId, {userId})` — admin-only via the route, `409` if the order already has a payment recorded (removing a discount only raises the total, so there's no shortfall risk; the restriction is about not undoing something the customer was already charged against), writes one `discount.remove` audit row |
| `src/seed.js` | 70 | `CATS`/`ITEMS` arrays and `seed()` (calls `migrate()`, then seeds categories/items/modifiers/tables/admin user if empty). (Phase 04) the `Kuah`/`Extra Lauk` groups are now created with explicit `min_select`/`max_select` (1/1 and 0/99 — the same values `migrations/004_menu.sql` backfills onto pre-existing rows) instead of inheriting the generic column defaults (0/1), and every kandar item is attached to both groups right after seeding — this attachment can't come from the migration's own backfill, since on a fresh install `seed()` inserts items *after* `migrate()` already ran against an empty `items` table |
| `migrations/001_baseline.sql` | 90 | The former `schema.sql`, verbatim, plus the `available` column `ALTER`. Applied by `src/db.js`'s `migrate()`. Idempotent — safe against a database that already has these tables |
| `migrations/002_money.sql` | 17 | Adds the eight bill columns to `orders` (`subtotal_cents`, `service_charge_cents`, `tax_cents`, `discount_cents`, `rounding_cents`, `total_cents`, `tax_rate_bp`, `svc_rate_bp`), backfills already-paid orders from `pay_total_cents`, seeds `settings` with `tax_rate_bp=600` / `svc_rate_bp=0` (`ON CONFLICT DO NOTHING`) |
| `migrations/003_integrity.sql` | 52 | Adds `orders.opened_by`/`paid_by`/`closed_by` and `order_items.added_by`/`voided_at`/`voided_by`/`void_reason` (all `INT REFERENCES users(id)` where applicable); creates `audit_log` (`id`, `at`, `user_id`, `action`, `entity_type`, `entity_id`, `detail jsonb`) + its two indexes; a `DO $$ ... $$` block cancels any pre-existing duplicate open orders per table (keeping the newest, logging the rest to `audit_log`) before `CREATE UNIQUE INDEX one_open_order_per_table ON orders (table_id) WHERE status NOT IN ('paid','cancelled')` |
| `migrations/004_menu.sql` | 21 | Creates `item_modifier_groups` (`item_id`, `group_id`, `sort`, PK on the pair); adds `modifier_groups.min_select`/`max_select` (defaults 0/1 — only meaningful per-mode after the two backfill `UPDATE`s below) and `items.sold_out_until DATE`; backfills every kandar item onto both existing groups (`WHERE i.kandar`, `ON CONFLICT DO NOTHING`) and sets `min_select=1, max_select=1` for `radio` groups / `min_select=0, max_select=99` for `checkbox` groups. This backfill only reaches rows that exist *at migration time* — `src/seed.js` does the equivalent for a fresh install, since it creates items and the two demo groups itself, after `migrate()` has already run |
| `migrations/005_payments.sql` | 29 | Creates `payments` (`id`, `order_id`, `method`, `amount_cents CHECK (> 0)`, `tendered_cents` nullable/cash-only, `taken_by`, `at`) + its index, and `discounts` (`id`, `order_id`, `kind` ∈ percent/amount/comp, `value`, `amount_cents` snapshotted, `reason`, `approved_by NOT NULL`, `at`); adds `order_items.seat`; backfills every already-`paid` order (that has a `pay_method` and no existing `payments` row) into a single `payments` row from its legacy `pay_method`/`total_cents`/`pay_total_cents`/`paid_by`/`paid_at` columns |
| `public/index.html` | 292 | Staff app shell: markup only (4 tabs + 3 modals), a small inline `<style>` for dark mode / theme-toggle / layout tweaks not yet folded into `style.css`. Every interactive element carries `data-action` (+ `data-id` etc.) instead of `onclick`. Loads `api.js` (classic script, defines the global `API`) then `js/main.js` as `type="module"`. (Phase 02) the admin "SST Setting" toggle is now two rate inputs (`#tax-rate-input`, `#svc-rate-input`) + `#save-rates-btn`. (Phase 03) admin tab gained an "Activity Log" card (`#audit-log`). (Phase 04) the old "Kandar modal" is now the generic `#modal-bg` modifier-group modal with `id="mod-confirm-btn"` (disabled until every attached group's minimum is met) and `data-action="close-mod-modal"`/`"confirm-mods"`; admin tab gained "Categories" (`#admin-categories`, up/down `data-action="cat-move"`) and "Modifier Groups" (`#admin-groups` + a `#new-group-name`/`#new-group-mode`/`data-action="create-group"` mini-form) cards; "Menu Items" (renamed from "Menu Availability") now shows attach/detach group chips, up/down sort buttons, and a "Sold out today" checkbox alongside the existing indefinite-availability switch. (Phase 05) payment modal rebuilt: the three method buttons (unchanged text "Cash"/"Card"/"DuitNow / eWallet" — the e2e journey selects them by exact name) now pay the *full remaining balance*; `#pay-cash-row`'s `#cash-received-input` feeds that full-payment leg's `tendered`; new `#pay-amount-row` (`#pay-amount-input` + "Apply cash"/"Apply card"/"Apply eWallet" buttons) pays an arbitrary partial amount; new `data-action="split-evenly"`/`"split-by-seat"` buttons plus `#pay-split-result` (populated client-side by `pos.js`, not raw HTML in this file). **(Phase 05b)** cart panel's `#cart-totals` is now four rows (`#cart-subtotal-rm`, `#cart-svc-row`/`#cart-svc-rm`, `#cart-tax-rm`, `#cart-total-rm`) instead of one bare "Total", mirroring the payment modal's breakdown; payment modal gained a `data-action="open-discount-form"` "Discount" button revealing `#discount-form` (`#discount-kind` percent/amount/comp, `#discount-value`, `#discount-reason`, `#discount-pin-row` with `#discount-admin-name`/`#discount-admin-pin` shown for non-admin callers, `data-action="apply-discount"`/`"close-discount-form"`) — applied discounts (with reason and a `data-action="remove-discount"` button for admins) render into `#pay-details`, populated client-side by `pos.js`. **(Phase 06)** header gained `#conn-dot` (green/amber/red live-connection indicator, styled in this file's inline `<style>`). **(Phase 07)** `#offline-banner` (hidden by default, styled in this file's inline `<style>`) sits above the header — shown by `pos.js` whenever the outbox has entries and the browser is offline; loads `/sw.js` registration via `main.js` |
| `public/js/state.js` | 92 | The shared `state` object (`menu`, `tables`, `cart`, `selTable`, `activeCat`, `modItem` — renamed from `kandarItem` in phase 04, since the modal it drives is no longer kandar-specific — `pollTimer`, `pendingRemarkItem`) plus `$`, `fmt`, `esc`, `toast`. **(Phase 06)** `connectStream()`/`disconnectStream()` own the app's single `EventSource` (token passed as `?token=`, since `EventSource` can't set `Authorization`); reconnects with its own backoff (1s→2s→4s…capped 30s, not the browser's fixed ~3s retry) rather than relying on native auto-reconnect; updates `#conn-dot`; `onStreamEvent(fn)` lets a tab subscribe to (debounced 250ms, batched) events and decide for itself whether to refetch |
| `public/js/pos.js` | 667 | Everything POS-tab: table select, menu/cart rendering, the modifier-group + remark modals, send-to-kitchen, payment modal/flow. Owns the delegated click listeners for `#tab-pos`, `#modal-bg`, `#pay-modal`, `#remark-modal`. Exports `loadAll`, `renderTables`. **(Phase 06)** subscribes via `onStreamEvent` — refreshes the table grid, or (inside a table's workspace) re-runs `checkOpenOrder()`, whenever a stream event lands while the POS tab is active. **(Phase 07)** `sendOrder()` no longer awaits the network — it `enqueue()`s a create/append request onto `outbox.js` and returns immediately, marking the just-sent lines `l.sent = 'pending'` (renders dim with a 🕒 tag, not editable, excluded from the server-bill portion of the cart total the same way an unsent line is) until `checkOpenOrder()` reconciles them to true `sent` once the outbox confirms; whether to create vs. append is decided from the cached `liveOrder` rather than a fresh `GET /api/orders`, so sending never depends on the network being up. `onOutboxChange` triggers `updateOfflineBanner()` (shows `#offline-banner` only while genuinely offline with entries queued) and re-runs `checkOpenOrder()`. Payments (`processPay`, `paySplitShare`) and `voidLine` are never queued — each checks `navigator.onLine` up front and fails loudly ("Cannot take payment while offline" / "Cannot void a line while offline") rather than risk an unreconstructable cash discrepancy. (Phase 03) cart lines carry the `order_items` row `id` + `voided`/`void_reason`; `renderCart` shows voided lines struck-through (excluded from the total) with a `Void` button on un-voided sent lines — `voidLine()` prompts for a reason and calls the void route; `sendOrder()` now catches a `409` from `POST /api/orders` (another tablet won the `one_open_order_per_table` race) and retries as an append to the returned `order_id` instead of failing. (Phase 04) `addItem` routes to `openModifierModal` whenever `item.modifier_group_ids.length` (kandar is now display-only); `openModifierModal`/`confirmModifiers` (renamed from the kandar-specific pair) render every attached group by its actual `mode`/`min_select`/`max_select` — inputs carry `data-group="<id>"`; `updateModifierValidity()` (wired to the modal's `change` event) disables `#mod-confirm-btn` until each group's minimum is satisfied. (Phase 05) the whole client-side bill-preview duplication from phase 02 is gone — `renderPayModal()` just displays `subtotal`/`service_charge`/`tax`/`discount`/`grand_total`/`payments`/`amount_due`, all already computed server-side (`recomputeOrderBill` keeps them live). `refreshPayModal()` re-fetches and re-renders without the "food still cooking?" `confirm()` (that only fires once, in `openPayModal()` — calling it on every refresh was re-prompting on each split-payment leg, a bug caught in manual testing). `payFull`/`payAmount` POST to `/api/orders/:id/pay`; cart lines gained an optional `seat` number input (`setSeat`), sent as `seat` on send-to-kitchen. `pendingShares` holds a split view's amounts *fixed at split time* (`splitEvenlyUI`/`splitBySeatUI` populate it from `GET /api/orders/:id/split`) — `paySplitShare` pays one entry and removes it from the array, rather than re-deriving shares from the shrinking remaining balance (which would silently change the numbers the cashier was just shown). Cash payments never send an explicit `tendered` equal to the requested amount: cash can't physically be tendered in exact sen, so the server rounds a settling leg to the nearest 5 sen and treats it as exact — a forced `tendered = amount` would instead reject legs that need rounding up (caught in manual testing of the RM10÷3 split). **(Phase 05b)** `checkOpenOrder` now also keeps the module-level `liveOrder` (the full order object from `GET /api/orders`, reset on `selectTable`/`backToTables`) so `renderCart()` can mirror the payment modal's server-computed `subtotal`/`service_charge`/`tax`/`grand_total` instead of re-summing cart lines itself — lines not yet sent (no server figures yet) still fold their raw price into subtotal/total only, and a live order whose bill columns are still `null` (a QR order the kitchen hasn't touched yet) falls back to the old raw client-side sum rather than showing an incorrect zero. `renderPayModal()` also renders `o.discounts` (reason shown, a `data-action="remove-discount"` button for admins on an order with no payments yet) and calls `closeDiscountForm()` on every re-render; `openDiscountForm`/`closeDiscountForm`/`updateDiscountValueUI`/`applyDiscount`/`removeDiscount` drive the new discount control — `applyDiscount` runs the staff PIN → `POST /api/discounts/authorize` → token flow for non-admins before `POST /api/orders/:id/discounts`, same as admin does directly |
| `public/js/kitchen.js` | 57 | `refreshKitchen`, `setSt`; delegated click listener on `#tab-kitchen`. (Phase 03) a voided line on the active-orders ticket renders struck-through with a **VOID** marker so the line cook stops cooking it. **(Phase 06)** subscribes via `onStreamEvent` — refreshes the ticket the moment an order changes, while the kitchen tab is active, instead of waiting on the old 3s poll |
| `public/js/dashboard.js` | 16 | `refreshDashboard` |
| `public/js/admin.js` | 177 | `refreshAdmin`, `toggleAvail`, `toggleModAvail`; delegated `change` listener on `#tab-admin` for availability toggles plus (phase 02) a `click` listener for `data-action="save-rates"` — `saveRates()` PATCHes `{tax_rate_bp, svc_rate_bp}` (percentages × 100, rounded) to `/api/settings`. Replaces the old `toggleSST`/`#sst-toggle`. (Phase 03) `refreshAdmin` also loads `GET /api/admin/audit?limit=100` and renders it into `#audit-log` (time, user, action, entity, JSON-stringified detail). (Phase 04) `refreshAdmin` caches the last `GET /api/admin/menu` response in module-level `lastMenu` (so move/reorder handlers don't need a round-trip to know current order); `toggleItemGroup` attaches/detaches via `POST`/`DELETE /api/admin/item_modifier_groups`; `moveItem`/`moveCategory` swap two entries and re-PATCH `sort=0..N-1` for every item/category in the new order (simple and correct regardless of prior tie values, since every seeded row starts at the column's default `sort=0`); `toggleSoldOutToday` PATCHes `{sold_out_today}`; `updateGroupSelect`/`createGroup` manage `min_select`/`max_select` and new groups |
| `public/js/outbox.js` | 163 | **New in phase 07.** IndexedDB-backed write queue (`mamak-outbox` DB, `queue`/`failed` object stores) so order entry survives a dropped connection or a page reload. `enqueue({url, method, body})` stores the entry (with a fresh UUID as both its id and its `Idempotency-Key`) and returns immediately. `flush()` sends queued entries strictly in insertion order, one at a time — an append must never overtake the create it belongs to — stopping (to preserve order) on a network failure or `5xx`, and started on app load, on the browser's `online` event, and every 15s while entries remain (`startOutbox()`). A `409` on a queued create (`one_open_order_per_table`, phase 03) is converted in place into an append to the order the server names, and retried exactly once. Any other `4xx` moves the entry to the `failed` store instead of retrying forever. `onOutboxChange(fn)` notifies subscribers (`pos.js`) whenever the queue changes so they can re-render |
| `public/sw.js` | 65 | **New in phase 07.** Service worker: precaches the app shell (`index.html`, `/js/*`, `/api.js`, `/style.css`) on `install`; network-first for `/api/*` GETs (falls back to cache when offline — this is what keeps `GET /api/menu` usable offline once it's been fetched once), cache-first for everything else; never intercepts non-`GET` requests (POSTs are the outbox's job, caching one here would hide the very failures it exists to queue). `CACHE_VERSION` constant bumps the cache name on each deploy; `activate` deletes any cache not matching the current version |
| `public/js/nav.js` | 45 | `buildNav`, `switchTab`, `refreshLive` (tab-aware refresh dispatcher — **(Phase 06)** now only the 60s safety-poll backstop, no longer the primary update path); delegated click listener on `#nav` |
| `public/js/main.js` | 63 | Theme toggle, login/logout, `showApp`/`showLogin`, wires the poll timer, page init. Exposes `window.showLogin` because `api.js` (a classic script) calls it directly on a 401. **(Phase 06)** `showApp()` calls `connectStream()` and drops the poll interval from 3s to 60s (`refreshLive` is now just the belt-and-braces backstop for a wedged proxy); `showLogin()` calls `disconnectStream()`. **(Phase 07)** calls `outbox.js`'s `startOutbox()` and registers `/sw.js`, both unconditionally at module load (not gated behind login) — a reload while offline must still flush whatever the outbox is holding once the network returns |
| `public/customer/index.html` | 156 | QR self-order page shell. Local `<style>` holds only the rules that are genuinely different from `style.css` for this page (sticky category bar, customer item-btn layout, `#cart-bar` sub-selectors, mobile bottom-sheet modal, loading spinner) — everything duplicated with `style.css` (`.c-header`, `.c-wrap`, `.c-success*`, base `#cart-bar`, `.remark-presets`) was moved there instead. Does **not** load `api.js` (unused on this page; its 401 handler called a `showLogin()` that doesn't exist here). (Phase 04) the `#kandar-modal`'s confirm button gained `id="km-confirm-btn"` and its actions were renamed `data-action="close-mods"`/`"confirm-mods"` — the DOM ids (`kandar-modal`, `km-*`) themselves were left alone since they're not user-visible |
| `public/customer/customer.js` | 228 | Init from `/t/:token`, menu render, cart, modifier-group/remark modals, submit — `type="module"`, imports `$`/`fmt`/`esc` from `../js/state.js`. All interactive elements use `data-action`, one delegated listener on `document.body`. (Phase 04) same generalisation as `pos.js`: `addItem` routes on `item.modifier_group_ids.length`, not `kandar`; `openModifiers`/`confirmModifiers`/`updateModifierValidity` render and validate from the item's actual attached groups; the local `kandarItem` variable is renamed `modItem`; the "Customisable" badge in `renderItems` now reflects `modifier_group_ids.length`, not `kandar` |
| `public/api.js` | 57 | `API` fetch wrapper: bearer token in `localStorage`, `login`/`logout`, `get/post/patch/del`, `getBlobUrl` for authenticated QR PNGs. Loaded only by the staff app now. (Phase 03) a non-ok response's thrown `Error` now also carries `.status` and `.body` (the parsed JSON) so callers can react to a specific status (e.g. `pos.js`'s 409-race retry), not just read `.message` |
| `public/style.css` | 412 | The terracotta design system: tokens, buttons, login, app shell, cards, table grid, POS layout, cart, kitchen, dashboard, admin, modal, `.remark-presets`, toast, customer-page extras (now the authoritative versions — see `public/customer/index.html` above), responsive. **Keep this — extend, don't replace**. (Phase 04) added `.btn:disabled`/`.btn:disabled:hover` (grey background, `not-allowed` cursor) — needed once `#mod-confirm-btn`/`#km-confirm-btn` started toggling `disabled` |
| `docker-compose.yml` | 34 | `db` (postgres:16-alpine) + `app`. Secrets via `${VAR}` from `.env` |
| `Dockerfile` | 7 | Single stage, runs as root, `CMD ["node", "src/server.js"]`. Hardened in phase 11 |
| `test/helper.js` | ~35 | `withDb(fn)`: creates a random `test_<hex>` schema against `TEST_DATABASE_URL`, points `src/db.js` at it via `PGOPTIONS` search_path, runs `migrate()`, calls `fn(db)`, then drops the schema. Tests never touch `public` |
| `test/unit/smoke.test.js` | 107 | Three regression tests (`node --test`): `migrate()` idempotency, admin login + wrong-PIN 401, `GET /api/orders?mode=recent` 200 (audit #9). `startApp()` clears the require-cache for every module under `src/` (except `db.js`, which `withDb()` already refreshed) before re-requiring `src/server.js`, so the route/service/lib tree picks up the current test's pool instead of a previous test's closed one. (Phase 02) the idempotency test counts `migrations/*.sql` on disk rather than hardcoding `1`, so it keeps working as later phases add migration files |
| `test/unit/money.test.js` | 190 | Phase 02: pure-function tests for `roundHalfUp`, `lineTotal`, `computeBill` (tax-on-subtotal+svc, discount, 100-line float-drift check), `formatRM`, plus one integration test — pay an order by cash, assert the stored `orders` row's components sum to `total_cents` (checked in integer cents via a direct `db.query`, not on the RM floats the API returns for display), and that changing the live `tax_rate_bp` afterwards does not alter the already-paid order. Two of the phase prompt's example cash-rounding numbers (gross 1063→1060, 1067→1070) are arithmetically impossible for "nearest 5 sen" (max adjustment magnitude is 2, not 3) and are replaced with the mathematically correct expected values for the same inputs — see the comment above those two tests |
| `test/unit/orders.test.js` | 265 | Phase 03 integration tests, each starting a fresh app + seeding a staff and a kitchen admin user via `setup()`: a real concurrent-request race on `POST /api/orders` for one table asserts one `201`/one `409` (with the winner's id) and exactly one row in the DB; a voided line is excluded from `total` but still comes back from the API with `voided`/`void_reason`; void validation (400 without a reason, 403 for `kitchen`); the staff-may-void-only-while-`sent`, admin-any-time rule; every mutating action (`order.create`/`.append`/`.void_line`/`.status`/`.pay`/`.cancel`) writes exactly one `audit_log` row with the correct `user_id`; backward status transitions 403 for `kitchen`, 200 for `staff` |
| `test/unit/modifiers.test.js` | 126 | Phase 04: calls `buildOrderItems` directly against a self-contained fixture (its own category/items/groups, not the seeded demo menu) — radio group 0/1/2 selected (400/ok/400), checkbox `max_select=3` with 4 selected (400), an option from a group not attached to the item (400, message names both), sold-out-today (400) vs. `sold_out_until` in the past (ok), and a kandar item still accepting the exact payload shape (one radio + N checkboxes) the UI has always sent |
| `test/unit/billing.test.js` | 438 | Phase 05: a pure unit test for `splitEvenly` (1000÷3/7/6, each asserted to sum back to 1000 — the classic split-bill bug); integration tests over a live app — two partial `Card` payments summing exactly to the total settle the order (`amountDue` 0); a `Cash` payment tendering RM20 against a smaller due settles with correct change, while requesting more than due by `Card` is `400`; appending a line to an order that already has one payment is `409`; a 10% percent discount on an RM20 subtotal resolves to exactly 200 cents and tax is still computed on the *undiscounted* subtotal+service_charge; a `comp` discount zeroes the order, closes it with zero `payments` rows (the table's `CHECK (amount_cents > 0)` forbids a zero-value row) and writes one `discount.apply` audit row. **(Phase 05b)** the void/discount-vs-partial-payment boundary from item 1, both as three cases each (below the paid amount → `409`, nothing changes; exactly on it → order settles with an `order.settle` audit row; above it → allowed, correct remaining balance) — the "below" case reproduces the phase prompt's exact RM21.20/RM15.00 scenario and asserts the literal refusal message; plus admin removing a discount before any payment reverts the total and writes one `discount.remove` audit row |
| `test/unit/idempotency.test.js` | 159 | **New in phase 07.** Integration tests over a live app (same `startApp()`/`withDb` pattern as `orders.test.js`/`billing.test.js`, using the seeded admin directly since role isn't what's under test here): same `Idempotency-Key` on `POST /api/orders` twice → one row, both responses carry the same id (second is `200`, not `201`); the same race run concurrently → one row, no `500`; a repeated key on `POST /api/orders/:id/items` doesn't duplicate lines; reusing a create's key as an unrelated append's key isn't confused (separate columns/indexes) |
| `playwright.config.js` | 36 | E2E config: single worker, `webServer` runs `node test/e2e/reset-db.js && node src/server.js` against a dedicated `mamak_e2e` database, `launchOptions.executablePath` points at a sandbox-preinstalled Chromium when present (falls back to Playwright's own resolution otherwise) |
| `test/e2e/reset-db.js` | 26 | Drops + recreates the `mamak_e2e` database before the app boots, so every E2E run starts from the same seeded, orders-empty state |
| `test/e2e/journeys.spec.js` | 151 | The six journeys named in `_CONVENTIONS.md`. **Implemented:** staff login → order → kitchen → pay; QR customer order; **(phase 05b)** split bill; **(phase 07)** offline order reconciles. **Pending** (`test.skip`, with the phase that adds them): void a line (03), shift open → close (09). (Phase 05) the "pay" step now selects `{name:'Cash', exact:true}` — the payment modal gained a "pay a specific amount" section whose buttons ("Apply cash" etc.) also contain the substring "Cash", which broke Playwright's default (non-exact) name matching. **(Phase 05b)** the split-bill journey never advances the order past `sent`, so "Mark Paid" fires the "food still cooking?" `confirm()` — it registers one `page.on('dialog', ...)` handler up front (accepting confirms, and answering `'2'` to `splitEvenlyUI()`'s `prompt()`) rather than a `page.once` scoped to just the split step, since relying on Playwright's default dialog auto-dismissal for the earlier confirm proved unreliable. **(Phase 07)** the offline journey goes offline (`context.setOffline(true)`), sends orders for two tables (each renders "pending" and bumps `#offline-banner`'s count), restores the network, and asserts via a direct `GET /api/orders` that exactly one order landed per table with the right items — the "reload mid-outage" case from the phase prompt's manual Verify step isn't automated here (Playwright's `page.reload()` mid-test adds flakiness without adding coverage beyond what the outbox's own IndexedDB persistence already guarantees) |
| `.github/workflows/ci.yml` | ~25 | Node 20 + `postgres:16-alpine` service, `npm ci && npm test`, on push and PR. Does not yet run the Playwright suite |

## Database tables

`users` (id, name, role∈admin/staff/kitchen, pin_hash) ·
`sessions` (token PK, user_id, created_at) ·
`categories` (id, name, sort) ·
`items` (id, category_id, name, price_cents, kandar, available, sort, **(phase 04)** sold_out_until —
`available AND (sold_out_until IS NULL OR sold_out_until < today-in-KL)` is "orderable"
(`services/orders.js`'s `ORDERABLE_SQL`); `kandar` is display-only from this phase on, no longer
read by any validation or modal-selection logic) ·
`modifier_groups` (id, name, mode∈radio/checkbox, **(phase 04)** min_select, max_select) ·
`modifier_options` (id, group_id, name, price_cents, available, sort) ·
`item_modifier_groups` (item_id, group_id, sort — PK on the pair) — **(phase 04)** which groups an
item actually offers; `buildOrderItems` rejects any option whose group isn't attached, and any
attached group's selection count outside `[min_select, max_select]` ·
`tables` (id, name, qr_token) ·
`orders` (id, table_id, status∈sent/preparing/ready/served/paid/cancelled, source∈staff/qr, note, created_at, updated_at, paid_at, pay_method, pay_total_cents, **(phase 02)** subtotal_cents, service_charge_cents, tax_cents, discount_cents, rounding_cents, total_cents, tax_rate_bp, svc_rate_bp — **(phase 05)** these eight now stay live-recomputed (`billing.recomputeOrderBill`) on every mutation while the order is open, not just written once at payment; they only become a true frozen *snapshot* once the order is `paid` — the last two rate columns still specifically preserve the rates in effect at that moment, independent of the live `settings` row, **(phase 03)** opened_by, paid_by, closed_by — all `INT REFERENCES users(id)`; a partial unique index `one_open_order_per_table` on `(table_id) WHERE status NOT IN ('paid','cancelled')` enforces at most one open order per table, **(phase 07)** idempotency_key — nullable, a partial unique index (`uniq_orders_idem`) lets `POST /api/orders` recognise a retried create) ·
`order_items` (id, order_id, item_id, name, price_cents, qty, note, **(phase 03)** added_by, voided_at, voided_by, void_reason — a voided line is never deleted, just marked; excluded from every total, **(phase 05)** seat — optional, drives `splitBySeat`, **(phase 07)** idempotency_key — nullable, one derived sub-key (`` `${batchKey}:${index}` ``) per line, `uniq_order_items_idem` lets `POST /api/orders/:id/items` recognise a retried batch) ·
`order_item_mods` (id, order_item_id, name, price_cents) ·
`settings` (key PK, value) — holds `tax_rate_bp` (default `600` = 6% SST) and `svc_rate_bp`
(default `0`) as of phase 02; the previously-unused `sst_on` key is orphaned, unread by any route ·
`payments` (id, order_id, method, amount_cents `CHECK (> 0)`, tendered_cents nullable/cash-only, taken_by, at) —
**(phase 05)** one row per payment leg; an order is `paid` once `Σ amount_cents === total_cents` ·
`discounts` (id, order_id, kind∈percent/amount/comp, value, amount_cents snapshotted, reason,
approved_by `NOT NULL`, at) — **(phase 05)** `approved_by` is always an admin id, whether the caller
was admin or an authorized staff member ·
`audit_log` (id, at, user_id, action, entity_type, entity_id, detail jsonb) — **(phase 03)** one row per
mutating action (`order.create`/`.append`/`.void_line`/`.status`/`.cancel`/`.pay`/**(phase 05)**`discount.apply`/
**(phase 05b)**`order.settle`/`discount.remove`;
`shift.open`/`shift.close` are reserved for later phases), written by `writeAudit()` in
`src/services/orders.js`

**Design rule already in place — preserve it:** `order_items` and `order_item_mods`
snapshot `name` and `price_cents` at order time. Editing or deleting a menu item
never rewrites a historical bill. Every new table must follow the same principle.

**Fixed in phase 05b** (was: "known edge case, not guarded" in phase 05): voiding a
line or applying a discount could push `amountDue` negative when the order already
had a payment recorded. Both now preview the resulting total first
(`billing.guardAgainstShortfall`) and refuse with `409` rather than allow it; if the
new total lands exactly on what's already paid, the order settles instead
(`billing.settleIfMatchesPaid`).

## API surface

Public (no auth): `GET /api/menu` (items now `orderable`-filtered and carry `modifier_group_ids`;
groups carry `min_select`/`max_select`) · `GET /api/t/:token` · `POST /api/public/orders`
(rate-limited 20 / 10 min / IP, 201 on success, 409 `{error, order_id}` if the table
already has an open order) · `GET /t/:token` (customer page) · `GET /api/health`

Authenticated: `POST /api/login` (rate-limited 10 / 10 min / IP) · `POST /api/logout` ·
`GET /api/tables` (all roles, names only) · `GET /api/orders[?mode=recent]` (each order now
also carries `payments[]`, `discounts[]` and `amount_due`) ·
`POST /api/orders` (201, or 409 `{error, order_id}` on the one-open-order-per-table race;
**(phase 07)** an `Idempotency-Key` header makes a retry return the original `{id}` with 200
instead of a second order) ·
`POST /api/orders/:id/items` (409 if the order already has a payment recorded;
**(phase 07)** `Idempotency-Key` likewise makes a retried batch a no-op 200) ·
`POST /api/orders/:id/items/:lineId/void` (`{reason}`, 3-200 chars;
staff while `sent`, admin any time before paid/cancelled) · `PATCH /api/orders/:id` (status;
`TRANSITIONS` now includes backward moves, 403 for `kitchen`) ·
`POST /api/orders/:id/pay` (`{method, amount?, tendered?}` — `amount` RM, defaults to the full
remaining balance; response `{ok, paid, change, remaining, settled, bill:{...}}`) ·
`GET /api/orders/:id/split?ways=N|by=seat` (preview only) ·
`POST /api/discounts/authorize` (`{name,pin}` → `{token, expires_in}`, admin credentials only) ·
`POST /api/orders/:id/discounts` (`{kind,value,reason,authorize_token?}` — admin-only unless a
valid `authorize_token` from the line above is supplied) ·
`DELETE /api/orders/:id/discounts/:discountId` (admin-only; `409` if the order already has a
payment recorded) ·
`GET /api/summary` · `GET|PATCH /api/settings` ·
**(Phase 06)** `GET /api/stream` (SSE, roles admin/staff/kitchen; bearer token via `?token=`
since `EventSource` can't set headers; `Last-Event-ID`/`?since=` replays missed events)

Admin only: `GET /api/admin/menu` (now also returns `item_modifier_groups`) ·
`POST|PATCH|DELETE /api/admin/items` (`PATCH` also takes `sort`, `sold_out_today`) ·
`POST|PATCH /api/admin/categories` (`PATCH` takes `name`/`sort`) ·
`POST|PATCH|DELETE /api/admin/modifier_options` ·
`POST|PATCH /api/admin/modifier_groups` · `POST /api/admin/item_modifier_groups` (attach) ·
`DELETE /api/admin/item_modifier_groups/:itemId/:groupId` (detach) ·
`GET|POST /api/admin/tables` · `GET /api/admin/tables/:id/qr.png` ·
`GET|POST|DELETE /api/admin/users` · `GET /api/admin/audit[?limit=&entity_id=]`

## Conventions in the existing code

- `awaitH(fn)` wraps an authenticated route; `publicH(fn)` wraps a public one and
  hides internal error text. Thrown errors carrying `.status` become that response
  — throw them via `AppError(message, status)` (`src/lib/errors.js`).
- Auth is plain Express middleware: `router.get('/path', requireRole('admin'), awaitH(...))`.
  `requireRole(...roles)` (`src/lib/auth.js`) looks up the bearer session and 403s
  if the user's role isn't in the list; pass no roles to just require *a* session.
- Money is integer cents in the DB; `cents2rm`/`rm2cents` (`src/lib/money.js`)
  convert at the edges only.
- Client-side, every string rendered into HTML goes through `esc()` (`public/js/state.js`
  on the staff app, `../js/state.js` re-imported on the customer page). Dynamic
  markup uses `data-action`/`data-id` attributes plus one delegated `click` (or
  `change`) listener per container — never an inline `onclick`/`onchange`.
- Shared staff-app state (`menu`, `tables`, `cart`, `selTable`, `activeCat`, …) lives
  in the single `state` object exported by `public/js/state.js`; import and mutate
  `state.foo`, don't reassign a destructured local.

## Local development

```bash
docker compose up                      # full stack
# or against a local postgres:
DATABASE_URL=postgres://postgres:PASS@localhost:5432/postgres \
  ADMIN_PIN=1234 BASE_URL=http://localhost:3000 node src/server.js
```

Seeds on first boot: 6 categories, 25 items, 2 modifier groups, 14 tables, and an
`Admin` user with `ADMIN_PIN`. Boot runs `src/db.js`'s `migrate()` first, applying
`migrations/*.sql` in order.

```bash
npm test              # node --test against TEST_DATABASE_URL (default localhost:5432/postgres)
npx playwright test   # six E2E journeys against a dedicated mamak_e2e database (auto-booted)
```
